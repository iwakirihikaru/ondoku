const key='ondoku-results-v02';const el=document.getElementById('content');
function render(){const xs=JSON.parse(localStorage.getItem(key)||'[]');if(!xs.length){el.innerHTML='<div class="empty">この端末にはまだ記録がありません。</div>';return;}el.innerHTML=`<table><thead><tr><th>日時</th><th>番号</th><th>判定</th><th>時間</th><th>発声</th><th>本文一致</th></tr></thead><tbody>${xs.map(x=>`<tr><td>${new Date(x.createdAt).toLocaleString('ja-JP')}</td><td>${x.studentNo}</td><td><span class="badge ${x.status}">${x.status==='green'?'🟢 実施':x.status==='yellow'?'🟡 要確認':'🔴 未完了'}</span></td><td>${Math.round(x.durationSec||0)}秒</td><td>${Math.round((x.voiceRatio||0)*100)}%</td><td>${Math.round((x.similarity||0)*100)}%</td></tr>`).join('')}</tbody></table>`}
document.getElementById('reload').onclick=render;document.getElementById('clear').onclick=()=>{if(confirm('この端末の記録を削除しますか？')){localStorage.removeItem(key);render()}};render();
